# 🔍 게시글이 안 보이는 문제 해결

## 문제 진단 체크리스트:

### 1️⃣ **Firebase Console에서 데이터 확인**

**가장 먼저 확인해야 할 것:**

1. **https://console.firebase.google.com/** 접속
2. 프로젝트 선택: **capstone-project-6cd46**
3. **Firestore Database** → **데이터(Data)** 탭 클릭
4. **`posts` 컬렉션**이 있는지 확인
5. 컬렉션 안에 **문서(documents)**가 있는지 확인

#### ✅ 문서가 있다면:
→ Firestore에는 저장되었지만, 읽어오는 코드에 문제가 있음

#### ❌ 문서가 없다면:
→ 저장 자체가 안 되고 있음 (보안 규칙 재확인 필요)

---

### 2️⃣ **브라우저 콘솔 확인**

1. 웹사이트에서 **F12** 또는 **Cmd + Option + I** (Mac) 눌러서 개발자 도구 열기
2. **Console** 탭 확인
3. 게시글 작성 후 나타나는 메시지 확인:
   - `✅ 게시글 작성 완료: [문서 ID]` ← 이게 나타나야 함
4. 관리자로 게시판 열었을 때:
   - `📋 anonymous 게시판 불러오는 중... (관리자: true)` ← 이게 나타나야 함
   - `✅ X개의 게시글을 불러왔습니다.` ← 개수 확인

#### 만약 콘솔에 오류가 있다면:
→ 빨간색 오류 메시지를 알려주세요!

---

### 3️⃣ **Firestore Index 문제일 수 있음**

`orderBy`와 `where`를 함께 사용할 때 **복합 색인(Composite Index)**이 필요합니다.

#### 증상:
- 콘솔에 `The query requires an index` 오류 메시지

#### 해결:
1. 콘솔에 나타나는 **링크를 클릭**
2. Firebase Console에서 자동으로 색인 생성 페이지로 이동
3. **색인 만들기** 버튼 클릭
4. 1-2분 대기 (색인 생성 중...)
5. 상태가 **"사용 설정됨"**으로 바뀌면 완료

---

### 4️⃣ **빠른 테스트 방법**

게시판 페이지를 열고 다음을 확인하세요:

#### 일반 사용자 계정:
```
1. 익명 게시판 열기
2. 개발자 도구 Console 확인
3. "📋 anonymous 게시판 불러오는 중... (관리자: false)" 메시지 확인
4. "✅ X개의 게시글을 불러왔습니다." 메시지 확인
```

#### 관리자 계정 (s20011656@sjajeju.kr):
```
1. 익명 게시판 열기
2. 개발자 도구 Console 확인
3. "📋 anonymous 게시판 불러오는 중... (관리자: true)" 메시지 확인
4. "✅ X개의 게시글을 불러왔습니다." 메시지 확인
```

---

## 🔧 **임시 해결책: 보안 규칙 단순화**

만약 복잡한 규칙 때문에 문제가 발생한다면, 임시로 더 간단한 규칙을 사용하세요:

**Firebase Console → Firestore Database → 규칙(Rules)**에서:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 모든 컬렉션: 로그인한 사용자는 읽기/쓰기 가능 (테스트용)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

⚠️ **주의**: 이 규칙은 테스트용입니다. 나중에 더 안전한 규칙으로 바꿔야 합니다!

---

## 📝 **알려주세요:**

1. **Firebase Console의 posts 컬렉션에 문서가 있나요?**
2. **브라우저 콘솔에 어떤 메시지가 나타나나요?**
3. **오류 메시지가 있나요?**

이 정보를 알려주시면 정확한 해결책을 드리겠습니다! 🎯




